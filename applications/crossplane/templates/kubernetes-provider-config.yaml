{{ if .Values.kubernetes.connectionSecretName }}
apiVersion: kubernetes.crossplane.io/v1alpha1
kind: ProviderConfig
metadata:
  name: default
spec:
  credentials:
    source: Secret
    secretRef:
      namespace: crossplane-system
      name: {{ .Values.kubernetes.connectionSecretName }}
      key: kubeconfig
{{ end }}