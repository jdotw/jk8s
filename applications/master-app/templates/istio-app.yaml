---
apiVersion: v1
kind: Namespace
metadata:
  name: istio-system
---
apiVersion: v1
kind: Namespace
metadata:
  name: istio-ingress
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: istio-app
  annotations:
    argocd.argoproj.io/sync-wave: "-3"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: {{ .Values.masterApp.repoURL }}
    targetRevision: {{ .Values.masterApp.targetRevision }}
    path: "{{ .Values.masterApp.path }}/istio-app"

  destination:
    namespace: {{ .Values.argocd.namespace }}
    server: "{{ .Values.spec.destination.server }}"

  syncPolicy:
    automated:
      prune: true
