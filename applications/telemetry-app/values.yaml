# Required
dns:
  fqdn: ""
argocd:
  namespace: ""
jaeger:
  storage:
    es:
      enabled: false
      url: ""
      credentialsSecretName: ""
    inMemory:
      enabled: true

password: "IT WAS ARGO ALL ALONG"

# Defaults
spec:
  destination:
    server: https://kubernetes.default.svc

