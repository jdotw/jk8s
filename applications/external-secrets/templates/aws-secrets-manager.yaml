{{ if .Values.aws.secretsManager.argocdServiceAccountName }}
apiVersion: external-secrets.io/v1alpha1
kind: SecretStore
metadata:
  name: secrets-store-aws-sm
  namespace: {{ .Values.argocd.namespace }}
spec:
  provider:
    aws:
      service: SecretsManager
      region: "ap-southeast-2"
      auth:
        jwt:
          serviceAccountRef:
            name: {{ .Values.aws.secretsManager.argocdServiceAccountName }}
{{ end }}
{{ if .Values.aws.secretsManager.jaegerServiceAccountName }}
---            
apiVersion: external-secrets.io/v1alpha1
kind: SecretStore
metadata:
  name: secrets-store-aws-sm
  namespace: jaeger
spec:
  provider:
    aws:
      service: SecretsManager
      region: "ap-southeast-2"
      auth:
        jwt:
          serviceAccountRef:
            name: {{ .Values.aws.secretsManager.jaegerServiceAccountName }}
{{ end }}
{{ if .Values.aws.secretsManager.appServiceAccountName }}
---            
apiVersion: external-secrets.io/v1alpha1
kind: SecretStore
metadata:
  name: secrets-store-aws-sm
  namespace: app
spec:
  provider:
    aws:
      service: SecretsManager
      region: "ap-southeast-2"
      auth:
        jwt:
          serviceAccountRef:
            name: {{ .Values.aws.secretsManager.appServiceAccountName }}
{{ end }}