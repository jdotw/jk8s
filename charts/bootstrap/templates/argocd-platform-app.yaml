apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: platform
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: "{{ .Values.platform.platformApp.repoURL }}"
    targetRevision: "{{ .Values.platform.platformApp.targetRevision }}"
    path: "{{ .Values.platform.platformApp.appsPath }}/platform"
    helm:
      parameters:
        - name: "argocd.namespace"
          value: "{{ .Release.Namespace }}"
        - name: "helm.releaseName"
          value: "{{ .Release.Name }}"
      values: |
        {{ toYaml .Values.platform | nindent 8 }}
  destination:
    namespace: "{{ .Release.Namespace }}"
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      prune: true
