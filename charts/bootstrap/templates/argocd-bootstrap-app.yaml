apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: bootstrap-app
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: "{{ .Values.bootstrap.bootstrapApp.repoURL }}"
    targetRevision: "{{ .Values.bootstrap.bootstrapApp.targetRevision }}"
    path: "{{ .Values.bootstrap.bootstrapApp.appsPath }}/bootstrap-app"
    helm:
      parameters:
        - name: "argocd.namespace"
          value: "{{ .Release.Namespace }}"
        - name: "helm.releaseName"
          value: "{{ .Release.Name }}"
      values: |
        {{ toYaml .Values.bootstrap | nindent 8 }}
  destination:
    namespace: "{{ .Release.Namespace }}"
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      prune: true
