apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: master-app
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: "{{ .Values.jk8s.masterApp.repoURL }}"
    targetRevision: "{{ .Values.jk8s.masterApp.targetRevision }}"
    path: "{{ .Values.jk8s.masterApp.path }}/master-app"
    helm:
      parameters:
        - name: "argocd.namespace"
          value: "{{ .Release.Namespace }}"
        - name: "helm.releaseName"
          value: "{{ .Release.Name }}"
      values: |
        {{ toYaml .Values.jk8s | nindent 8 }}
  destination:
    namespace: "{{ .Release.Namespace }}"
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      prune: true
